Se acabaron las búsquedas. Entrega ahora el JSON final con el formato de las
instrucciones: un único objeto, sin texto alrededor y sin bloques de código.

Recuerda que **«sin datos» es una respuesta válida**: si no encontraste una
tasa publicada por la institución en alguna de las URLs que devolvieron las
búsquedas, contesta con `hallazgos` vacío y `sin_datos` en `true`. No rellenes
con nada que no venga de un resultado real.
